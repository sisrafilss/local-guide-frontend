export interface ITourist {
  id?: string;
  email: string;
  name: string;
  profilePicUrl?: string | null;
  // contactNumber: string;
  // address: string;
  // isDeleted: boolean;
  createdAt: string;
  updatedAt: string;
}
